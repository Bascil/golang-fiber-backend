export GOPATH=$HOME/projects/go-demos
export PATH=$PATH:/usr/local/go/bin:$GOPATH/bin

nano .bashrc

source .bashrc

go get github.com/oxequa/realize

type Claims struct {
	jwt.StandardClaims // Get all fields on standard claim
}

